import 'package:flutter/material.dart';

class RoadsScreen extends StatelessWidget {
  const RoadsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Roads Screen'),
    );
  }
}
